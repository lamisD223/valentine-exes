<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8">
<title>Lettre Ka-chaud 💌</title>
<style>
    body {
        margin: 0;
        font-family: Arial, sans-serif;
        background: linear-gradient(135deg, #ffdde1, #ee9ca7);
        height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
        overflow: hidden;
    }

    /* Lettre initiale */
    .letter-container {
        text-align: center;
    }

    .letter {
        width: 350px;
        height: 200px;
        background: #fff;
        border: 3px solid #f28ab2;
        border-radius: 10px;
        display: flex;
        justify-content: center;
        align-items: center;
        flex-direction: column;
        box-shadow: 0 5px 15px rgba(0,0,0,0.3);
        transition: all 1s ease;
    }

    button {
        margin: 10px;
        padding: 10px 20px;
        border: none;
        border-radius: 5px;
        background-color: #f28ab2;
        color: white;
        font-size: 16px;
        cursor: pointer;
        transition: all 0.3s ease;
    }

    /* Scène romantique */
    .scene {
        position: relative;
        width: 700px;
        height: 300px;
        margin-top: 20px;
        display: none;
    }

    .car {
        position: absolute;
        bottom: 50px;
        width: 200px;
        transition: transform 2s ease;
    }

    .sally {
        left: 50px;
        animation: moveToKiss 3s forwards;
    }

    .mcqueen {
        right: 50px;
    }

    @keyframes moveToKiss {
        0% { transform: translateX(0); }
        100% { transform: translateX(180px); }
    }

    .message {
        position: absolute;
        bottom: 10px;
        width: 100%;
        text-align: center;
        font-size: 20px;
        color: white;
        font-weight: bold;
        opacity: 0;
        animation: fadeIn 2s 3s forwards;
    }

    @keyframes fadeIn {
        to { opacity: 1; }
    }

    /* GROSSE pluie de cœurs */
    .rain {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        pointer-events: none;
        overflow: hidden;
        z-index: 999;
        display: none;
    }

    .rain span {
        position: absolute;
        top: -50px;
        font-size: 20px;
        animation: fall linear infinite;
        opacity: 0.8;
    }

    /* Pluie dense */
    .rain span:nth-child(1) { left: 5%; animation-duration: 5s; animation-delay: 0s; }
    .rain span:nth-child(2) { left: 10%; animation-duration: 4.8s; animation-delay: 0.3s; }
    .rain span:nth-child(3) { left: 15%; animation-duration: 6s; animation-delay: 1s; }
    .rain span:nth-child(4) { left: 20%; animation-duration: 5.2s; animation-delay: 0.7s; }
    .rain span:nth-child(5) { left: 25%; animation-duration: 4.5s; animation-delay: 1.2s; }
    .rain span:nth-child(6) { left: 30%; animation-duration: 5.8s; animation-delay: 0.3s; }
    .rain span:nth-child(7) { left: 35%; animation-duration: 6.1s; animation-delay: 1.7s; }
    .rain span:nth-child(8) { left: 40%; animation-duration: 4.9s; animation-delay: 0.5s; }
    .rain span:nth-child(9) { left: 45%; animation-duration: 5.5s; animation-delay: 1.5s; }
    .rain span:nth-child(10) { left: 50%; animation-duration: 4.2s; animation-delay: 0.2s; }
    .rain span:nth-child(11) { left: 55%; animation-duration: 5.6s; animation-delay: 0.8s; }
    .rain span:nth-child(12) { left: 60%; animation-duration: 6.3s; animation-delay: 1.1s; }
    .rain span:nth-child(13) { left: 65%; animation-duration: 5.1s; animation-delay: 0.6s; }
    .rain span:nth-child(14) { left: 70%; animation-duration: 4.7s; animation-delay: 0.9s; }
    .rain span:nth-child(15) { left: 75%; animation-duration: 5.9s; animation-delay: 1.3s; }
    .rain span:nth-child(16) { left: 80%; animation-duration: 6.5s; animation-delay: 0.4s; }
    .rain span:nth-child(17) { left: 85%; animation-duration: 4.8s; animation-delay: 1.0s; }
    .rain span:nth-child(18) { left: 90%; animation-duration: 5.4s; animation-delay: 0.6s; }
    .rain span:nth-child(19) { left: 95%; animation-duration: 6.2s; animation-delay: 1.2s; }
    .rain span:nth-child(20) { left: 100%; animation-duration: 5s; animation-delay: 0.8s; }

    @keyframes fall {
        0% { transform: translateY(0) rotate(0deg); opacity: 0.8; }
        100% { transform: translateY(100vh) rotate(360deg); opacity: 0; }
    }
</style>
</head>
<body>

<div class="letter-container" id="letterBox">
    <div class="letter">
        <p>Es ce que tu es Ka‑chaud ?</p>
        <button id="yesBtn">Oui</button>
        <button id="noBtn">Non</button>
    </div>
</div>

<!-- Pluie de cœurs -->
<div class="rain" id="rain">
  <span>❤️</span><span>❤️</span><span>❤️</span><span>❤️</span><span>❤️</span>
  <span>❤️</span><span>❤️</span><span>❤️</span><span>❤️</span><span>❤️</span>
  <span>❤️</span><span>❤️</span><span>❤️</span><span>❤️</span><span>❤️</span>
  <span>❤️</span><span>❤️</span><span>❤️</span><span>❤️</span><span>❤️</span>
</div>

<!-- Scène romantique -->
<div class="scene" id="scene">
    <img class="car sally" src="sally.png" alt="Sally">
    <img class="car mcqueen" src="mcqueen.png" alt="Lightning McQueen">

    <div class="message">
        Happy Valentine’s Day 💕<br>
        To the most beautiful thing that’s happened to me in years.
    </div>
</div>

<script>
    const yesBtn = document.getElementById('yesBtn');
    const noBtn = document.getElementById('noBtn');
    const letterBox = document.getElementById('letterBox');
    const scene = document.getElementById('scene');
    const rain = document.getElementById('rain');

    let yesSize = 16; // taille initiale du bouton Oui

    yesBtn.addEventListener('click', () => {
        letterBox.style.display = 'none';
        scene.style.display = 'block';
        rain.style.display = 'block';
    });

    noBtn.addEventListener('click', () => {
        yesSize += 5; // augmente le bouton Oui à chaque Non
        yesBtn.style.fontSize = yesSize + 'px';
    });
</script>

</body>
</html>
